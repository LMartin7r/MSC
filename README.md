This is where you can find all the Supplementary Tables, that cannot fit in the thesis work.
ROMEmalevsfemale.xlsx contains all the candidate DMRs. This is the output file that the RoAM computational pipeline gives.
all_with_delta.xlsx contains all the candidate DMRs in a descending order, where the first one has the biggest Max Qt value.
top50_promoter_gene_cgi.xlsx contains the top 50 DMRs in descending order, where the first one has the biggest Max Qt value, but in this table all the DMRs are in the Gene body, the Promoter region and are in CPG islands.
